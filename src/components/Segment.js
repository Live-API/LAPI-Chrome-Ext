import React from 'react'
import { Button, Input, Segment } from 'semantic-ui-react'

const SegmentExampleRaised = () => (
  <Segment raised>
    <Input placeholder='Please name your prop' />
    <Button>Save</Button>
  </Segment>
)

export default SegmentExampleRaised